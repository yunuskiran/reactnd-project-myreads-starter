import React from "react";

export const PageNotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      <h2>Upss! Page Not Found!</h2>
    </div>
  );
};
